import 'package:flutter/material.dart';

class BuktiSetorScreen extends StatelessWidget {
  const BuktiSetorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: 
      ListView(
        children: [

        ],
      );
     
    );
  }
}
